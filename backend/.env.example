# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database
MONGODB_URI=mongodb://localhost:27017/loginapp

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-here

# Session Secret (generate a strong random string)
SESSION_SECRET=your-super-secret-session-key-here

# SMTP Email Configuration
SMTP_PROVIDER=gmail  # Options: gmail, outlook, mailtrap, custom
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password  # For Gmail, use App Password (16 characters)
SMTP_FROM=your-email@gmail.com  # Optional, defaults to SMTP_USER
APP_NAME=Dyanpitt

# For other providers:
# Mailtrap: SMTP_PROVIDER=mailtrap, SMTP_USER=mailtrap-username, SMTP_PASS=mailtrap-password
# Outlook: SMTP_PROVIDER=outlook, SMTP_USER=your-email@outlook.com, SMTP_PASS=your-password
# Custom: SMTP_PROVIDER=custom, SMTP_HOST=smtp.example.com, SMTP_PORT=587, SMTP_SECURE=false