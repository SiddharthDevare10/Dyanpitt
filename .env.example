# Frontend Environment Variables

# API Base URL (Backend server URL)
VITE_API_URL=http://localhost:5000/api

# App Configuration
VITE_APP_NAME=Dyanpitt App
VITE_APP_VERSION=1.0.0

# Feature Flags (Frontend)
# Enable React Query provider (default false)
VITE_REACT_QUERY_ENABLED=false
# Enable React Query Devtools in development (default false)
VITE_REACT_QUERY_DEVTOOLS=false
# Enable ky client in apiService (default false)
VITE_USE_KY_CLIENT=false
# Enable react-hook-form + zod in MembershipDetailsScreen (default false)
VITE_RHF_ENABLED_MEMBERSHIP=false
# Enable date-fns-tz in istUtils (default false)
VITE_USE_DATEFNS_TZ=false

# Note: OTPs will be displayed in the backend terminal (no email needed)